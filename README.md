# 🧠 Smart Form Filler (LangGraph + Streamlit + LangSmith)

Smart Form Filler is a deterministic, schema-driven form-filling application that guides users through structured forms by asking one question at a time, validating inputs, and generating a final summary.

The project is built using **LangGraph state machines** for workflow control, **Streamlit** for the UI, and **LangSmith** for tracing and observability.

---

## ✨ Key Features

- 📋 Multi-form support (Leave Request, Expense Claim)
- 🔁 Step-by-step question flow driven by schema
- ✅ Field-level validation (dates, ranges, amounts, yes/no)
- 🧠 Deterministic workflow (no LLM hallucinations)
- 🕵️ End-to-end tracing with LangSmith
- 📂 Form history with save and replay
- 📄 Auto-generated form summary
- 🧩 Easily extensible with new forms and fields



